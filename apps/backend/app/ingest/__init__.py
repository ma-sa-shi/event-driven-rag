"""ingest-fnのドキュメント取込処理。

テキスト抽出 → チャンク分割 → Embedding生成 → S3 Vectors登録を担当する。
worker イメージにはLangChain系ライブラリを入れない方針(ADR-0003)のため、
このパッケージはapp/ragに依存しない。逆向きの参照は制約に触れないため、
埋め込みの次元数と応答の解釈はchat-fn側がここから取り込んで共有する。
"""
